#!/bin/sh -e
CONF=@CONFIG@
if [ ! -e "$CONF" ]; then
  echo "no config: $CONF"
  exit 1
fi
SHARE="/usr/share/cusp-conf/"

echo "proposed fstab changes"
echo
gen-cusp-fstab
echo

cat "${SHARE}world" >> "/etc/apk/world"
cp "${SHARE}lbu.list" "/etc/apk/protected_paths.d/lbu.list"
